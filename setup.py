#!/usr/bin/env python
from setuptools import setup

setup(name='ATGUiAutomation',
      description='ATGUiAutomation',
      install_requires=[
          'nose >= 1.3.7',
          'setuptools >= 40.8.0',
          'urllib3 >= 1.24.1',
          'selenium >= 3.141.0'
      ]
      )
